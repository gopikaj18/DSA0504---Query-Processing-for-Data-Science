import pandas as pd

df = pd.DataFrame({
    'school_code': ['s001', 's002', 's001', 's002', 's003'],
    'age': [15, 16, 14, 15, 17]
})

result = df.groupby('school_code')['age'].agg(['mean', 'min', 'max'])

print(result)
